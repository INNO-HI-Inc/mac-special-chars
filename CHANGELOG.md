# 변경 기록

## 미출시

### 배포 — GitHub Pages

저장소를 받지 않아도 웹에서 바로 내려받아 설치할 수 있게 했습니다.
<https://inno-hi-inc.github.io/mac-special-chars/>

- `template/landing_template.html` — 다운로드 랜딩 페이지.
  타이핑 데모, 3단계 설치 안내, 필수 기호 미리보기(눌러서 복사), 팔레트 안내, FAQ.
  기호 개수·버전·plist 크기는 빌드 때 `entries.json` 에서 채워집니다.
- `scripts/site.py` — `_site/` 조립 (랜딩 · 치트시트 · plist · 스크린샷).
  `.local.` 산출물이 섞이면 빌드를 멈춥니다. `--check` 로 검증만 할 수 있습니다.
- `.github/workflows/pages.yml` — `main` 푸시마다
  `build.py --check` → `smoke.py` → `site.py` → Pages 배포.
- 치트시트에 랜딩으로 돌아가는 링크 추가 (사이트 빌드에서만 삽입).

## 1.1.0 — 2026-08-25

팔레트를 크게 손봤습니다. 개인 항목(연락처·서명)을 공개 저장소와 분리하는
체계를 만들고, 검색·키보드·UI를 확장했습니다.

### 구조
- 팔레트 HTML과 lua를 `build.py` 문자열에서 `template/` 파일로 분리
  (`build.py` 481 → 131줄). 이제 UI를 직접 편집할 수 있습니다.
- `config.json` + `config.local.json` 설정 체계. 빌드 시 lua·팔레트에 주입됩니다.
- 한글→영문 별칭 사전을 `scripts/alias.py` 로 분리해 치트시트와 팔레트가 공유합니다.

### 개인 항목 분리
- `entries.local.json` (git 제외) 에 넣은 항목은 `.local.` 산출물에만 병합됩니다.
  공개 `entries.json` · `plist` · `index.html` · 팔레트에는 들어가지 않습니다.
- 개인 항목은 「최근」 기록에도 남지 않습니다 (`privacy.excludePersonalFromRecent`).
- 팔레트에서 「개인」 섹션이 맨 위에 옵니다.

### 빌드
- `--check` 검증만 · `--watch` 자동 재빌드 · `--quiet`
- 스키마·카테고리·단축어 문자 검증, 같은 기호 중복 경고, 변경 요약 출력
- `scripts/smoke.py` 스모크 테스트 (개인정보 격리 검사 포함)
- `scripts/textreplace.py` 시스템 텍스트 대치 등록 현황 비교·백업

### 팔레트 — 검색
- 영문 별칭 검색 (`star` → ★). 이전 판은 데이터가 비어 있어 동작하지 않았습니다.
- 유니코드 코드포인트 검색 (`U+2605`)
- 점수 기반 랭킹 — 단축어 정확 일치가 맨 앞으로
- 결과 개수 표시, 빈 결과일 때 비슷한 항목 제안
- 검색어 히스토리 (⌥↑ / ⌥↓), 지우기 버튼, ⌘⌫

### 팔레트 — 키보드
- Tab / ⇧Tab, Home / End, PageUp / PageDown
- ⌥1…9 섹션 점프, ⌃1…9 N번째 즉시 입력
- ⇧Enter 연속 입력(안 닫힘), ⌥Enter 이름 복사, ⌃Enter 즐겨찾기
- Esc 2단계 (검색어 있으면 지우기 → 없으면 닫기)
- `?` 로 단축키 도움말

### 팔레트 — UI
- 즐겨찾기(핀), 자주 쓰는 항목(빈도순), 섹션 접기 — 상태는 저장됩니다
- 테마 · 글자 크기 · 단축어 배지 토글
- 복사/입력 토스트, 고대비 모드, `prefers-reduced-motion` 대응

### 입력
- 긴 문자열(4자 이상)은 키스트로크 대신 붙여넣기로 입력합니다.
  주소·계좌번호처럼 긴 항목이 안정적으로 들어갑니다.
- 연속 입력 시 이전 클립보드를 복원합니다.
- 마우스 커서가 있는 모니터에 팔레트를 띄웁니다.
- 핫키를 `config.json` 에서 바꿀 수 있습니다.

### 고친 것
- **Esc로 팔레트가 안 닫히던 문제.** 한글 입력기가 켜져 있으면 macOS가 Esc를
  IME 조합 취소로 먼저 소비해서 웹뷰 키 핸들러까지 오지 않았습니다.
  Hammerspoon 네이티브 핫키로 잡도록 바꿨습니다.
- **팔레트가 포커스를 잃어도 떠 있으면서 다른 앱의 Esc까지 가로채던 문제.**
  포커스가 떠나면 팔레트를 닫습니다.
- **`install.sh` 가 인감·서명 로컬 빌드를 조용히 덮어쓰던 문제.**
  `build_local.py` 와 같은 파일에 쓰기 때문에 인감·서명 기능이 사라졌습니다.
  이제 감지하면 멈추고 `FORCE=1` 을 요구하며, 덮어쓰기 전에 백업합니다.
- 기호 개수가 99로 하드코딩돼 실제와 어긋나던 문제 → 런타임 계산.

### 추가
- `ㅁ이노하이` → 【이노하이】
- `uninstall.sh`

---

## 1.0.0 — 2026-07-08
- 텍스트 대치 99종 + ⌥Space 팔레트 + 웹 치트시트
- 인감·서명 기능을 로컬 전용으로 분리
